<% location ||= @location %>
<tr>
  <td><%= location.id %></td>
  <td><%= location.name %></td>
  <td><%= location.state %></td>
  <td><%= location.city %></td>
  <td><%= location.street_first %></td>
  <td><%= location.street_second %></td>
  <td><%= location.default_location %></td>
  <td><%= location.opening_time.strftime("%I:%M%p") %></td>
  <td><%= location.closing_time.strftime("%I:%M%p") %></td>
  <td>
    <div class="btn-group">
      <%= link_to 'Show', admin_location_path(location), class: 'btn btn-info' %>
      <%= link_to 'Inventory', admin_location_inventory_items_path(location), class: 'btn btn-primary' %>
      <%= link_to 'Edit', edit_admin_location_path(location), class: 'btn btn-warning' %>
      <%= link_to 'Destroy', admin_location_path(location), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
    </div>
  </td>
</tr>
